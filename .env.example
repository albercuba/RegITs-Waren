APP_SECRET_KEY=replace-with-a-long-random-secret
ADMIN_PASSWORD=change-this-admin-password

# Optional fallback SMTP settings.
# If admin settings are saved in the database, those settings are used instead.
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_SENDER_EMAIL=
SMTP_RECIPIENT_EMAIL=
SMTP_USE_TLS=true
